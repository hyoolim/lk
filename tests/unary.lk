include('tester.lk')
Tester/new('Unary Operators')/do {

/test('!', !True, False)
# /test('@', false ? 1 ! 2, 2)
/test('$', $[ 1, 2, 3 ], '1 2 3 ')
/test('-', - -5, 5)
# /test('+', true/to-s, 'true')
/finish
}
